# Google Vision API Key (optional - falls back to smart simulation if not set)
GOOGLE_VISION_API_KEY=your_key_here

# Google Maps API Key (optional - falls back to geo simulation if not set)
GOOGLE_MAPS_API_KEY=your_key_here

PORT=5000
